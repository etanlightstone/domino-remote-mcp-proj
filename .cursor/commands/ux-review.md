# ux-review

Please do a UX review of the UI {provided-image-file}. 

Use @.cursor/rules/usability_design_principles.mdc Provide an annotated screenshot afterwards that reference the UX findings and guidance in a generated markdown file report. Be sure the annotated screenshot is embedded in the markdown report. The generated annotated screenshot should be copied to the same directory as this project and embedded in the markdown report. If reviewing an html file rather than an image, use a browser tool to visit and interact and assess and take screenshots from.